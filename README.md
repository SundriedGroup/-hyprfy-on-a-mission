# Hyprfy — On a Mission desktop-responsive build

This keeps the mobile-first UI and adds a desktop workspace at 900px+.

Replace the corresponding files in the existing GitHub repo and push to main.
Keep your existing Vercel environment variables unchanged.
